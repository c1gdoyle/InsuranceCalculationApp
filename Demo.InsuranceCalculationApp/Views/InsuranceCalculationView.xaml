<UserControl x:Class="Demo.InsuranceCalculationApp.Views.InsuranceCalculationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Demo.InsuranceCalculationApp.Views"
             xmlns:dP="clr-namespace:Demo.Presentation.Behaviours;assembly=Demo.Presentation"
             dP:ViewControllerBehaviour.Controller="{Binding}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Path=Title}" />
        <Button Grid.Row="1"
                Content="Exit"
                Command="{Binding Path=ExitCommand}" />

    </Grid>
</UserControl>
